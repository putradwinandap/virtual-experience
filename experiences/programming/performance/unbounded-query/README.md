# Unbounded Query

Queries that are acceptable on small datasets can become a dominant cost when result size or scanned rows grows without a bound.

## Scenarios

- [The query that became the hot path](scenarios/query-grows-with-traffic.md)
